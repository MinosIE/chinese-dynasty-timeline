# 中华王朝时间轴 · 产品需求文档（PRD）

> 版本：v1.0（MVP 已上线）　|　日期：2026-09-16　|　状态：公网部署中

---

## 1. 产品定位

一个**轻量、零依赖、单文件**的中国历史朝代科普网页。以「时间轴 + 可折叠卡片」的形式，把从夏到清的主要朝代、帝王世系、核心人才与关键制度浓缩在一页之内，帮助普通读者在 5 分钟内建立「三千年王朝脉络」的框架性认知。

- **Slogan**：中华王朝 · 千年脉络
- **一句话**：一张网页，看懂三千年权力、制度与文明的流转。

## 2. 目标用户

| 用户 | 诉求 |
| --- | --- |
| 中学生 / 历史初学者 | 快速建立朝代顺序与关键人物框架 |
| 家长 / 老师 | 给孩子讲历史的入门级可视化素材 |
| 普通国人 | 对「上下五千年」有朴素兴趣但缺乏系统梳理 |

## 3. 核心价值

- **一页看全**：把分散的朝代信息整合为单一可滚动时间轴。
- **可深可浅**：卡片默认折叠，概览 / 深挖自由切换。
- **零门槛**：纯静态单文件，手机 / 电脑 / 平板均可用，无需登录。
- **免费可分享**：部署在 GitHub Pages，任何人都可访问、转发。

## 4. 功能需求

### 4.1 已上线（MVP）
- **F1 朝代时间轴**：按时间顺序排列节点，左侧彩线串联。
- **F2 大时代筛选**：全部 / 先秦 / 秦汉 / 三国两晋南北朝 / 隋唐 / 宋元 / 明清。
- **F3 可折叠卡片**：概览 → 帝王世系 / 核心人才 / 政策制度 / 一句话总结。
- **F4 数据维度**：朝代（起止·都城·存续年数）、帝王（名号·在位·年号/庙号/谥号·备注）、人才（文臣/武将/思想文人/其他分类）、政策标签、总结。
- **F5 响应式 & 动效**：移动端适配 + 滚动入场动画（尊重 `prefers-reduced-motion`）。
- **F6 科普免责声明**：标注数据为概览、以权威史料为准。

### 4.2 规划中（Roadmap / v1.1+）
- **F7** 顶部总览统计 + 朝代存续时长可视化（甘特 / 条形）。
- **F8** 搜索 / 关键词定位（帝王、人物、制度）。
- **F9** 单朝代分享卡片 / 截图导出。
- **F10** 大事件时间标注（安史之乱、靖康之变等）。
- **F11** 黑暗模式 / 主题切换。
- **F12** 多语言（繁体 / 英文）切换。
- **F13** 数据外置为 JSON，便于社区共建与校对。
- **F14** 朝代疆域缩略图 / 关系图谱。

## 5. 信息架构

```
Hero（标题 + KPI）
  └─ 大时代 Tab 筛选
       └─ 时间轴卡片列表（夏 → 清）
            └─ 页脚说明（免责声明）
```

卡片内部层级：`概览(feature)` → `帝王世系(emperors)` → `核心人才(talents by role)` → `政策制度(policies)` → `总结(summary)`。

## 6. 内容准确性规范（科普红线）

- 页面明确标注「数据为科普概览，细节以权威史料为准」。
- 传说 / 半信史（夏、商部分君主）标注 `approx` 与「信史待考」。
- 并存政权（三国、南北朝等）不简单相加年数，注明「以下年数不相加」。
- 仅作中性历史陈述，不做政治定性。
- 数据后续若外置，需可溯源（引用《史记》《资治通鉴》等）。

## 7. 设计与交互原则

- **视觉语言**：中国红 `#D4351C` + 深蓝墨 `#0A2540` + 金 `#C79A3A`，呼应「王朝」气质。
- **单文件优先**：HTML + CSS + JS 内联；仅动效引用 CDN（motion@11 ESM）。
- **性能**：无构建步骤，首屏即内容；动效可选、可降级。
- **可访问性**：语义标签、对比度达标、支持 `prefers-reduced-motion`。

## 8. 技术架构

- 纯静态站点，单文件 `index.html`（原名 `chinese-dynasty-timeline.html`）。
- 数据以 JS 数组内联（`DATA` 常量）。
- 渲染：原生 JS 模板字符串 + 折叠交互。
- 动效：`motion@11`（ESM CDN），可选降级。
- 部署：GitHub Pages（main 分支根目录）。

## 9. 验收标准

- [x] 桌面 / 移动端布局正常，无横向滚动。
- [x] 6 个筛选 Tab 正确过滤。
- [x] 16 个朝代卡片折叠 / 展开正常。
- [x] 数据渲染无缺失、无错位。
- [x] `prefers-reduced-motion` 下无动画且内容可见。
- [ ] 部署后公网可访问（进行中）。

## 10. 部署（GitHub Pages）

- **仓库**：`github.com/MinosIE/chinese-dynasty-timeline`
- **分支**：`main`；根目录 `index.html`
- **启用**：仓库 `Settings → Pages → Source: Deploy from a branch → main / (root)` → Save
- **访问地址**：`https://MinosIE.github.io/chinese-dynasty-timeline/`

## 11. 迭代路线图

| 版本 | 内容 |
| --- | --- |
| v1.0（当前） | MVP 上线、公网可访问 |
| v1.1 | 搜索 + 存续时长可视化 + 数据外置 JSON |
| v1.2 | 大事件标注 + 主题切换 + 分享卡片 |
| v2.0 | 社区共建数据 + 多语言 + 互动测验 |
